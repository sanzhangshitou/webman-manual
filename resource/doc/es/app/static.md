# Archivos estáticos
Los archivos estáticos se colocan bajo `plugin/{plugin}/public`.
Por ejemplo, al acceder a `http://127.0.0.1:8787/app/foo/avatar.png` se obtiene en realidad el archivo `plugin/foo/public/avatar.png`.
