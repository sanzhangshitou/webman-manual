# 기본 플러그인

기본 플러그인은 일반적으로 공통 컴포넌트로, 보통 Composer로 설치하며 코드는 vendor 디렉터리 아래에 위치합니다. 설치 시 사용자 정의 설정(미들웨어, 프로세스, 라우트 등)을 `{메인 프로젝트}config/plugin` 디렉터리로 자동 복사할 수 있습니다. webman은 이 디렉터리의 설정을 자동으로 인식하여 메인 설정에 병합하므로, 플러그인이 webman의 생명 주기 어느 단계에든 개입할 수 있습니다.

자세한 내용은 [기본 플러그인 생성](create.md)을 참조하세요.
