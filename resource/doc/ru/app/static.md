# Статические файлы
Статические файлы размещаются в `plugin/{плагин}/public`.
Например, при обращении к `http://127.0.0.1:8787/app/foo/avatar.png` фактически возвращается файл `plugin/foo/public/avatar.png`.
