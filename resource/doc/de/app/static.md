# Statische Dateien
Statische Dateien werden unter `plugin/{plugin}/public` abgelegt.
Beim Zugriff auf `http://127.0.0.1:8787/app/foo/avatar.png` wird beispielsweise tatsächlich die Datei `plugin/foo/public/avatar.png` abgerufen.
