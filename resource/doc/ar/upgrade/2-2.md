# دليل الترقية 2.2

## الميزات والتغييرات

* دعم تعليقات المسار (route annotations)

**يرجى عمل نسخة احتياطية قبل الترقية، ثم تنفيذ الأوامر التالية للترقية**
```
composer config -g --unset repos.packagist
composer require -W workerman/workerman:~5.1 workerman/webman-framework:~2.2
```
